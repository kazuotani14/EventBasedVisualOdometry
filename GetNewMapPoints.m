%This function will look for high density places in the H_planes, 
% and return these points in 3D for addition to the map

function [new_map_points] = GetNewMapPoints(H_planes, kf_pose)
	new_map_points = [];
end

% FindClusters is a helper to find clusters on each plane
function [found_cluster_image] = FindClusters(H_plane)
	found_cluster_image = [];
end